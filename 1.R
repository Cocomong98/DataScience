# 어떤 걸 데이터를 통해서 알고자 하는가?
# 이메일 데이터가 있을 때, 스팸인지 아닌지 등 

# 입력해주는 변수를 입력변수, 설명변수라 한다 
# 우리가 알고자 하는 정보를 나타내는 변수, 데이터를 목적변수(target variable)라고 한다
# 입력변수로 목적변수를 알아내는 과정을 공부한다. 

# classificaton

# regression

# 과거 기록을 바탕으로 예측 (무릎 아플 때 100변 중 90번 비 옴 = 그럼 어지간하면 맞다)

# 전날 상한가를 친 주식을 샀을 때 (10번 정도), 어떤 날은 다음날 5% 오르고, 3%오르고 등의 경우가 나옴
# 그것들의 평균이 1.1% 상승이라면, 전날 상한가 주식을 샀을 때 1.1% 상승이라고 볼 수 있다

# 단일 변수 모델은 결과 예측에 하나의 변수만 쓴다
# 다중 변수 모델은 하나 이상의 변수를 쓴다 (결정 트리. k 근접 이웃, 베이스 모델)